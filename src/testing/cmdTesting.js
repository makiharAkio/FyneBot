const { Client } = require('discord.js');

/**
 *
 * @param {Client} client
 * @param {} 
 */
module.exports = {
    name: '',
    description: '',
    deleted: true,
  
    callback: async (client, ) => {
      try {
          l
  
      } catch (error) {
        console.error;
      }
    }
  };